#!/usr/bin/env bash
# moonlight — Moonlighter control CLI. Symlink this into your PATH, or run it directly.
set -uo pipefail
DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec python3 "$DIR/lib/cli.py" "$@"
